(begin
    (input double in1)
    (define out (fold + in1 (list 0 1 2 3)))
    (observe out out))

; Test 1
; inject in1 0
; stabilize
; expect out 6

; Test 2
; inject in1 -6
; stabilize
; expect out 0

; Test 3
; inject in1 1
; stabilize
; expect out 7
